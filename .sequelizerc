const path = require('path')
const dir = 'src'

module.exports = {
    'config': path.resolve(dir, 'config/config.json'),
    'models-path': path.resolve(dir, 'app/models'),
    'seeders-path': path.resolve(dir, 'seeders'),
    'migrations-path': path.resolve(dir, 'migrations'),
}